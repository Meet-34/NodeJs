events is a built in node.js module
EventEmitter is a class inside the event module

Creating an object of Event Emiiter class using new
booking is an instance of event emitter class

booking object can now:
listen to events -> .on 
emit (trigger) events -> .emit 

One object can listen to multiple events 

